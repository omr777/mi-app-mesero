name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ICONOS
  cupertino_icons: ^1.0.8
  
  # RED (Para PokeAPI)
  http: ^1.2.1

  # FIREBASE CORE (Obligatorio)
  firebase_core: ^3.6.0

  # BASE DE DATOS (¡NUEVO!)
  # Este es el paquete para usar la base de datos en tiempo real
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true